{
  "name": "markbook-classic",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run sidecar:build:debug && bun --cwd apps/desktop dev",
    "build": "bun --cwd apps/desktop build",
    "sidecar:build:debug": "bun --cwd rust/markbookd sidecar:build:debug",
    "sidecar:build:release": "bun --cwd rust/markbookd sidecar:build:release",
    "test:e2e": "bun run sidecar:build:debug && bun --cwd apps/desktop build:renderer && bun x playwright test",
    "test:packaging": "node apps/desktop/scripts/smoke-packaged-dir.cjs",
    "test:e2e:packaged": "node apps/desktop/scripts/smoke-packaged-dir.cjs && MARKBOOK_RUN_PACKAGED_E2E=1 bun x playwright test apps/desktop/e2e/packaged-smoke.e2e.spec.cjs",
    "test:reports": "bun test packages/reports/src/__tests__/render_smoke.test.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.1",
    "playwright": "^1.50.1"
  }
}
